function mostrar()
{
	let vMax = 10;  let vMin = 1; let vNum;

	//Ej:                 0.0               9           1
	vNum = Math.round(Math.random() * (vMax - vMin) + vMin);

	alert(vNum);

}//FIN DE LA FUNCIÓN